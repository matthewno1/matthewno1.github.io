类的封装：类的使用和实现被分离，实现的细节被封装并对用户隐藏。
类的关系：关联，聚合，组合
基本数据类型的对象包装：java.lang中：Integer, Double, Character, Boolean,Float,Byte,Short, Long
Integer方法：Integer(value: int) Integer(s: String) typeValue(); valueOf(s:string); parseInt(s:String);toString();compareTo(Integter):
装箱：基本数据类型到包装类：相反则开箱。
String类：构造字符串：String newString = new String(string1); string1可为字符数组可为纯字符串。
字符串的替换和分割：replace(oldChar, newChar); replaceFirst(oldeString, new String); replaceALl();; split( String);
字符串和数组的转换：字符串到数组：toCharArray();数组到字符串:String(char[]), valueOf(Char[])
字符和数值转化为字符串: valueOf();
StringBuilder:单任务 StringgBuffer:多任务并发访问
StringBuilder(s:String)
append, insert, replace, reverse, setCharAt,delete, deleteCharAt;
toString();capacity(); charAt(); length(); setLenght(); subString(); trimToSize();
