声明数组变量： elementType[] array;
创建数组： array = new elementType[arraySize];
数值型默认值 '\u0000' boolean 默认为false;
访问数组下标：array[index];
数组初始化：elementType[] arrayRefVar = {value0, value1};数组初始化必须声明，创建，初始化在一条语句中.
foreach : for( elementType element : arrayRefVar){}
数组的复制：循环语句赋值，arraycopy, clone;  arraycopy(sourceArray, srcPos, targetArray, tarpos,length),  
数组传递方法：匿名数组 new elementType[]{value1....valuek};
可变长参数列表：typeName... parameterName
数组的查找：二分查找和线性查找
Arrays类：java.util.Arrays.sort(numbers);Arrays.sort(numbers,1,3);Arrays.binarySearch(list,l1);equals(kust1,list2);fill(list2,1,5,8);toString(list2);
向main方法传递字符串： java TestMain arg0 arg1 arg2 当arg有空格则用""括住字符串
